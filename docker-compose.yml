services:

  postgres:
    build:
      context: ./infra/postgres
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      POSTGRES_DB:            ${POSTGRES_DB}
      POSTGRES_USER:          ${POSTGRES_USER}
      POSTGRES_PASSWORD:      ${POSTGRES_PASSWORD}
      PGDATA:                 ${PGDATA:-/var/lib/postgresql/data}
      DB_POSTGRESDB_PASSWORD: ${DB_POSTGRESDB_PASSWORD}
      MB_DB_PASS:             ${MB_DB_PASS}
    command: >
      postgres
        -c shared_preload_libraries=pg_cron
        -c cron.database_name=${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
      - postgres_backups:/backups
    networks:
      - default
      - easypanel
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  metabase:
    image: metabase/metabase:latest
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      MB_DB_TYPE:              postgres
      MB_DB_HOST:              postgres
      MB_DB_PORT:              5432
      MB_DB_DBNAME:            ${MB_DB_DBNAME:-metabase_app}
      MB_DB_USER:              ${MB_DB_USER}
      MB_DB_PASS:              ${MB_DB_PASS}
      MB_SITE_URL:             ${MB_SITE_URL}
      MB_EMBEDDING_SECRET_KEY: ${MB_EMBEDDING_SECRET_KEY}
      JAVA_TIMEZONE:           America/Bogota
      MB_SEND_EMAIL_ON_FIRST_LOGIN: ${MB_SEND_EMAIL_ON_FIRST_LOGIN:-false}
    volumes:
      - metabase_data:/metabase-data
    networks:
      - default
      - easypanel
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=easypanel"
      - "traefik.http.routers.metabase-http.rule=Host(`${MB_DOMAIN}`)"
      - "traefik.http.routers.metabase-http.entrypoints=http"
      - "traefik.http.routers.metabase-http.middlewares=redirect-to-https@file,bad-gateway-error-page@file"
      - "traefik.http.routers.metabase-https.rule=Host(`${MB_DOMAIN}`)"
      - "traefik.http.routers.metabase-https.entrypoints=https"
      - "traefik.http.routers.metabase-https.tls=true"
      - "traefik.http.routers.metabase-https.tls.certresolver=letsencrypt"
      - "traefik.http.routers.metabase-https.middlewares=bad-gateway-error-page@file"
      - "traefik.http.services.metabase.loadbalancer.server.port=3000"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:3000/api/health || exit 1"]
      interval: 60s
      timeout: 20s
      retries: 5
      start_period: 120s

  # ── Servicios desactivados temporalmente ─────────────────────────────────────
  # n8n:       migrado a instancia general externa (mismo servidor, red easypanel)
  # uptime-kuma: desactivado hasta estabilizar el stack
  # portal:    desactivado hasta estabilizar el stack

volumes:
  postgres_data:
    driver: local
  postgres_backups:
    driver: local
  n8n_data:
    driver: local
  metabase_data:
    driver: local
  kuma_data:
    driver: local
  portal_data:
    driver: local

networks:
  default:
    driver: bridge
  easypanel:
    external: true
